# plugins/varnish.yaml
plugin:
  plugin_schema_version: "1.0"
  name: "Varnish TLS Scanner"
  version: "1.0.0"
  author: "CipherIQ Team"
  category: "cache"
  description: "Detects Varnish instances and extracts TLS configuration"
  priority: 50

detection:
  methods:
    - type: binary
      paths:
        - "/usr/sbin/varnishd"
        - "/usr/local/sbin/varnishd"
      required: true
      confidence: 0.95

    - type: process
      names:
        - "varnishd"

    - type: port
      ports: [80]
      protocol: "tcp"
      check_ssl: true
      validate_process: true
      expected_processes:
        - "varnishd"

    - type: config_file
      paths:
        - "/etc/varnish/config"
        - "/etc/varnish/varnish.conf"
        - "/opt/varnish/conf/config"
      required: false

    - type: package
      package_names:
        - "varnish"
      server_packages:
        - "varnish"
      confidence: 0.90

config_extraction:
  files:
    - path: "${DETECTED_CONFIG_DIR}/config"
      parser: "custom"
      crypto_directives:
        - key: "ssl.enabled"
          type: "boolean"
          maps_to: "service.tls_enabled"

        - key: "ssl.certificate"
          type: "path"
          resolve: true
          maps_to: "certificate.path"

        - key: "ssl.key"
          type: "path"
          resolve: true
          maps_to: "private_key.path"

        - key: "ssl.ca_cert"
          type: "path"
          resolve: true
          maps_to: "ca_cert.path"

        - key: "ssl.protocols"
          type: "string"
          maps_to: "protocol.min_version"

        - key: "ssl.ciphers"
          type: "string_list"
          separator: ":"
          maps_to: "cipher_suites"
