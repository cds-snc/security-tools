# plugins/jetty.yaml
plugin:
  plugin_schema_version: "1.0"
  name: "Jetty TLS Scanner"
  version: "1.0.0"
  author: "CipherIQ Team"
  category: "application_server"
  description: "Detects Jetty instances and extracts TLS configuration"
  priority: 50

detection:
  methods:
    # Method 1: Detect by binary executable (Phases 1-2)
    - type: binary
      paths:
        - "/usr/share/jetty9/start.jar"
        - "/opt/jetty/start.jar"
      required: true
      confidence: 0.95

    - type: process
      names:
        - "java"

    - type: port
      ports: [8443]
      protocol: "tcp"
      check_ssl: true

    - type: config_file
      paths:
        - "/etc/jetty/config"
        - "/etc/jetty/jetty.conf"
        - "/opt/jetty/conf/config"
      required: false

    # Detect by package (Phase 2)
    - type: package
      package_names:
        - "jetty9"
        - "jetty"
      server_packages:
        - "jetty9"
      confidence: 0.90


config_extraction:
  files:
    - path: "${DETECTED_CONFIG_DIR}/config"
      parser: "custom"
      crypto_directives:
        - key: "ssl.enabled"
          type: "boolean"
          maps_to: "service.tls_enabled"

        - key: "ssl.certificate"
          type: "path"
          resolve: true
          maps_to: "certificate.path"

        - key: "ssl.key"
          type: "path"
          resolve: true
          maps_to: "private_key.path"

        - key: "ssl.ca_cert"
          type: "path"
          resolve: true
          maps_to: "ca_cert.path"

        - key: "ssl.protocols"
          type: "string"
          maps_to: "protocol.min_version"

        - key: "ssl.ciphers"
          type: "string_list"
          separator: ":"
          maps_to: "cipher_suites"
