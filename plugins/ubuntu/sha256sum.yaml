plugin:
  plugin_schema_version: "1.0"
  name: "sha256sum Crypto Scanner"
  version: "1.0.0"
  author: "CipherIQ Team"
  category: "hash_utility"
  description: "GNU Coreutils SHA-256 hash utility - internal implementation (sha256_init_ctx)"
  priority: 50
  requires_cbom_version: "1.3.0"

detection:
  methods:
    - type: binary
      paths:
        - "/usr/bin/sha256sum"
        - "/bin/sha256sum"
      required: true
      confidence: 0.98
