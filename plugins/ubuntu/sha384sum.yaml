plugin:
  plugin_schema_version: "1.0"
  name: "sha384sum Crypto Scanner"
  version: "1.0.0"
  author: "CipherIQ Team"
  category: "hash_utility"
  description: "GNU Coreutils SHA-384 hash utility - internal implementation (sha384_init_ctx)"
  priority: 50
  requires_cbom_version: "1.3.0"

detection:
  methods:
    - type: binary
      paths:
        - "/usr/bin/sha384sum"
        - "/bin/sha384sum"
      required: true
      confidence: 0.98
